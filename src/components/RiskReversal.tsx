export default function RiskReversal() {
  return (
    <section className="risk" aria-label="Our guarantee">
      <div className="wrap">
        <div className="risk-card">
          <span className="risk-eyebrow">Our guarantee</span>
          <p className="risk-copy">
            If the first build doesn&apos;t fit how you coach, <em>we rebuild it with you</em> — no extra cost.
          </p>
        </div>
      </div>
    </section>
  );
}
